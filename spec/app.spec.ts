import {expect} from 'chai';
import { Redxjs } from '../src/index';
describe('Demo', () => {
  beforeEach(function() {

  })
  it('should add correctly', () => {
    expect(10).to.not.equal('Demo Result');
  });

});aklsdjflasdj